# Keel

여러 프로젝트에서 기본으로 사용하는 공통 앱 프레임워크다.

## 운영 경계

- 특정 소비 프로젝트의 업무 기능을 공통 코어에 넣지 않는다.
- 공통 기능과 앱별 확장 지점을 구분하고, 단일 사용처만을 위한 추상화를 추가하지 않는다.
- 소비 앱은 Keel 버전을 선택한다. Keel 변경만으로 소비 앱을 자동 갱신하거나 배포하지 않는다.
- 외부 소스를 도입할 때 출처와 라이선스를 확인하고 필요한 고지를 보존한다.
- 공개 저장소에 비밀값, 개인 경로, 비공개 프로젝트 내용과 사용자 데이터를 넣지 않는다.

## 작업 진입 및 완료

1. 글로벌 `~/.claude/CLAUDE.md`와 이 문서를 읽는다.
2. 작업에 관련된 `docs/PROGRESS.md`, `docs/CHANGELOG.md`, 설계 결정을 확인한다.
3. Git 상태를 확인하고 사용자 변경을 보존한다.
4. 완료 및 커밋 전에 글로벌 `update-docs` 스킬로 문서를 점검한다.
5. 검증된 빌드·테스트 명령이 마련되면 이 문서에 기록한다. 없는 명령을 추정해 안내하지 않는다.

## 문서와 게시

- 상태는 `docs/PROGRESS.md`, 이력은 `docs/CHANGELOG.md`, 결정은 `docs/adr/`가 소유한다.
- GOLEM은 프로젝트 식별자와 위치를 관리한다. 상태를 카탈로그에 복제하지 않는다.
- GoldenNote 연결은 기본 활성화한다. 게시 범위는 공개 README·설계·사용법·변경 이력에 한정한다.
- 소비 앱의 비공개 코드·데이터·운영 정보는 GoldenNote 게시 대상에서 제외한다.

## 검증 명령

`experiments/app-base-comparison/`에서 의존성 설치 후 `npm --prefix orca run build`,
`npm --prefix theia run build`, `npm test`를 실행한다. 비교 예제용 명령이며 제품 배포 명령이 아니다.
설치와 macOS 개발용 서명 보정은 해당 디렉터리의 `README.md`를 따른다.
